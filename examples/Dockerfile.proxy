FROM python:3.12-slim

WORKDIR /opt/agent_metering

COPY pyproject.toml README.md requirements.txt ./
COPY agent_metering ./agent_metering

RUN pip install --no-cache-dir .

EXPOSE 8787

CMD ["uvicorn", "agent_metering.proxy:app", "--host", "0.0.0.0", "--port", "8787"]
